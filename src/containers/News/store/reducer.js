const initialState = {
  isFetched: false,
  data: null
}

const newsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default newsReducer
